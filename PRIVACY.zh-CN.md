# Soundloom 隐私政策——开发预览版

简体中文 · [English](PRIVACY.md)

**生效日期：** 尚未生效<br>
**最后更新：** 2026 年 8 月 23 日

本文描述当前 Soundloom 开发原型，属于待审核的发布候选文档，并非已经生效的 App Store 隐私政策。公开发布前，必须补充合法开发者身份、联系地址、最终 App 行为和 App Store 分发信息。

## 摘要

Soundloom 默认把录音和衍生笔记保存在 App 的本地容器中。项目不要求注册 Soundloom 账号；当前原型不包含广告、分析或跨 App 跟踪 SDK。

以下两条处理路径可能让数据离开设备：

1. 当前原型没有要求设备端识别，因此 Apple Speech framework 可能通过 Apple 的语音识别服务处理音频。
2. 只有当你主动请求 AI 整理时，Soundloom 才会把转写文本和整理指令发送到你配置的 AI 服务商与服务地址。

## Soundloom 处理的数据

### 录音

当你在 iPhone 上开始录音时，Soundloom 会在本地 App 容器中创建音频文件。通过受支持 iPhone 系统入口导入的音频会被复制到本地录音收件箱。

Soundloom 不会故意把原始录音上传给 App 中配置的 AI 整理服务商。你请求转写时，Apple Speech 可能访问该录音，具体见下文。

### 转写与整理结果

转写文本、处理状态、摘要、时间线、主题、参与者标签和待办会与录音索引一起保存在本地。这些字段可能包含从录音内容中提取的个人信息或敏感信息。

### AI 服务商凭据与设置

你在 Soundloom 中输入的 API Key 会保存在 Apple Keychain。服务商名称、模型标识、服务地址、启用状态和当前服务商选择会保存在本地 App 偏好设置中。Soundloom 不会故意把 API Key 写入录音、导出文件或 AI 请求正文。

### 诊断信息

原型可能把音频会话状态和失败信息写入 Apple 统一日志。代码设计上不会记录录音内容、转写文本或 API Key。公开分发前仍须重新审计 Release 构建日志。

## 数据何时离开设备

### Apple Speech 转写

当前转写请求把 `requiresOnDeviceRecognition` 设置为 `false`。根据设备、所选语言、网络、服务可用性和 Apple 的具体实现，录音音频及相关识别数据可能由 Apple 处理。Apple 对数据的处理受其条款和隐私政策约束。

不能因为录音保存在本地，就推断转写过程一定离线完成。

### 可选 AI 整理

只有当你选中一条记录并主动请求整理时，Soundloom 才会把数据发送给 AI 服务商。当前请求包括：

- 转写文本；
- 描述所选记录模式和目标 JSON 结构的指令；
- 所选服务商要求的模型标识。

请求使用你提供的 API Key，由设备直接发送到已配置的服务地址。服务商可能根据自己的条款和隐私政策保留、处理或使用数据。Soundloom 无法控制第三方服务商的数据保留方式。发送敏感文本前，请先阅读对应服务商的政策。

当前配置模型中包含 DeepSeek、通义千问/DashScope、Kimi/Moonshot、OpenAI、Anthropic、Gemini 和 OpenRouter。并非每个服务商都已经在当前原型中实现专用请求适配器。

## 权限

Soundloom 可能请求：

- **麦克风权限**：创建录音；
- **语音识别权限**：转写录音。

你可以在系统“设置”中修改麦克风和语音识别权限。撤销权限会阻止相关功能，但不会自动删除已有文件。

## 保留与删除

开发原型会在本地保留录音、索引、转写和整理结果，直到 App 行为、App 容器移除或操作系统将其删除。

当前原型**尚未**提供完整的单条录音删除或“清除全部本地数据”App 内入口。这是公开发布的阻塞项。在删除流程完成并验证前，不应把本政策作为 App Store 的正式隐私政策发布。

存储层已经具备移除 API Key 的能力，但公开发布前必须验证用户可以从界面访问该能力。Keychain 项目的系统行为可能与普通 App 容器文件不同，包括重新安装后的行为。发布版必须提供明确的凭据删除入口。

## 账号、跟踪和数据出售

当前原型：

- 不要求注册 Soundloom 账号；
- 不包含广告 SDK；
- 不会故意跨 App 或网站跟踪用户；
- 不出售个人数据；
- 不运营接收录音或转写文本的 Soundloom 服务端。

用户自行选择和配置的第三方 AI 服务商属于独立服务。

## 录制他人

不同地区和场景对录音同意的要求不同。你有责任取得必要同意，并合法使用 Soundloom。Soundloom 不用于隐蔽录音、电话拦截或持续监控。

## 儿童

Soundloom 不面向儿童，当前原型也不会通过 Soundloom 自营服务故意收集儿童信息。但录音仍可能包含其他人的信息，使用时应取得适当同意并谨慎处理。

## 安全

Soundloom 使用 Apple App Sandbox 保存本地文件，使用 Keychain 保存服务商凭据，并在配置为 `https://` 地址时通过加密 HTTPS 连接通信。任何存储或传输方式都无法保证绝对安全。自定义服务地址由用户控制；使用不安全或不可信地址可能导致数据泄露。

## 政策变更

当 iPhone App 获得独立 Xcode 工程、删除入口、最终转写策略、分发信息或新增服务时，本政策会相应调整。重要隐私变化应写入 Release Notes，并同步更新 App Store 隐私披露。

## 联系方式

如需咨询开发仓库相关的隐私问题或提出隐私请求，请发送邮件至 [krystalwy10@gmail.com](mailto:krystalwy10@gmail.com)。在双方先行约定安全传递方式之前，请勿发送真实录音、转写、API Key 或其他敏感个人信息。
